# DEVLOG.md — 旧电脑 · 迭代日志

> 本项目迭代历史的**唯一**记录处（2026-07-25 起，由 AGENTS.md「当前状态」与 PROJECT_PLAN.md 第 8 节合并而来，此前两处重复记录）。
> 规则：**只增不删，新条目加在顶部，新→旧排列**。agent 平时无需通读，查历史用 Grep 关键词定位、命中再读该行。体积阈值：到 ~500KB 或跨年时按年归档（旧年切到 `DEVLOG-<年份>.md`），主文件只留当年。
> 格式：`- **YYYY-MM-DD｜标题**：正文`。技术细节要留住（根因、关键数值、文件名），这是换机/换 agent 接力的上下文；已蒸馏成规则的坑同时抄送 `PITFALLS.md`。

## 2026

- **2026-08-09｜文档效率整改：晋级纪律堵漏 + AGENTS 瘦身**：动因=npm 镜像规则 08-07 已记 DEVLOG、当日却仍用默认源白等 11 分钟——DEVLOG 平时不读，**藏在日记里的规则等于没记**。整改（用户拍板）：① 铁律 6 扩为「经验当天晋级」——代码坑 → PITFALLS，环境/操作类经验 → AGENTS 环境备忘；② npm 镜像规则补进环境备忘（腾讯 > 阿里）；③ 文档地图 + DEVLOG 头部补「查历史用 Grep 关键词定位」；④ AGENTS 瘦身：收尾清单删 2026-07-25 故事段、img2threejs 冗条压缩、当前阶段条目压回摘要级（细节全在 DEVLOG）——15.4→14.8KB（大头是当前阶段压回摘要级；高信号区——架构速览/环境备忘主体——不动，再瘦要砍真规则，不建议）；⑤ DEVLOG 立归档阈值：~500KB 或跨年时按年切 `DEVLOG-<年份>.md`。
- **2026-08-09｜展览馆「返回停在刚看的展品」：大厅 `?focus=` 机制（playwright-core 7 断言全过）**：痛点=展品页整页跳转返回后大厅轮播永远回首件。修法（`exhibits/src/App.vue` 三处）：① 返回链接 `./index.html` → `./index.html?focus=<exName>`；② 大厅 `current` 初始值读 `?focus=`（id 找不到回退 0）；③ 切画 `watch(current)` 写 `history.replaceState`（不产生历史条目），浏览器返回键/分享链接也能回到刚看的那幅。验收：一次性脚本 `../tools/accept-focus`（playwright-core 驱动本机 chromium-1234，用完即删）7 断言——返回链接带参 / 点返回后亮灯画对 / 切画后 URL focus 跟随 / 直开 `?focus=galaxy` 生效 / 无参默认仍首件 / 零 pageerror。坑复演：npm 默认 registry 卡 11 分钟才装上（本机该走腾讯/阿里镜像，优先级见 08-07 条目）。
- **2026-08-09｜展品 005「云桥」：新展品类型「会动的画」首件，图生视频 + ffmpeg 减速补帧进馆**：动因=用户想让封面画"活"又不愿背视频体积；**GIF 方案被数据证伪**（256 色+无帧间压缩，同内容比 mp4 大一个数量级）→ 定案"无声循环小视频"（`<video muted autoplay loop playsinline>`，观感=GIF，业界标准做法）。**循环工艺分级（用户拍板，以后按此执行）**：无方向运动画面（光影/摇曳/微视差）→ 正反打 ping-pong 无缝循环（`split+reverse+concat`）；有方向运动画面（人物行走/水流/雨雪）→ 不反转，**减速拉长 + 接受硬接缝**。生成走 dreamina CLI `image2video`（积分制，两版）：v1 `seedance2.0mini` 45 积分——提示词"保持站立不行走"钉不住，人物被放走（沿桥缓行 5% 画面宽），用户判退；v2 `seedance2.0_vip` 70 积分——提示词加码"伫立原地/双脚不动/位置始终不变"（正向描述法），过。**坑：image2video 钉人物姿态，"不做某动作"式否定约束效力弱，要写死正向姿态词；mini 档服从度明显不如 vip**。ffmpeg 后处理（四版人眼对比选定）：0.8x/0.7x/0.6x 纯 setpts（有效帧率 19.3/16.9/14.5fps，0.6x 已现顿挫）→ 定版 **0.6x + `minterpolate` 运动补偿补帧回真 24fps**（`setpts=PTS/0.6,minterpolate=fps=24:mi_mode=mci:mc_mode=aobmc:me_mode=bidir:vsbmc=1`，人物发丝/栏杆硬边缘零变形；`-an -c:v libx264 -preset slow -crf 27 -movflags +faststart`）——5.04s/10MB 原料 → **8.3s/800KB 成品**。**展品页 `cloudbridge.vue`**：海报帧先进（静画兜底），`@playing` 后活画层 1.6s 淡入覆盖——"画活了"；大厅同款深木画框+卡纸内衬 CSS（墙上那幅走进去还是那幅）；manifest 005，大厅 4:3 封面 = poster 中心裁 960×720。素材三件套 `public/covers/`（mp4 800KB + poster 102KB + cover 82KB）。源图 = 即梦两候选（16:9 选定：人物占比小翻车率低+镜水道微动素材；1:1 落选留 temp）。验收：build 通过 + Playwright 截图 PC/移动/大厅（环形轮播左邻已见云桥封面）+ 三资源 curl 200；全程用户人眼判决（模型档/速度档均用户拍板）。母带 v2-raw.mp4 与档案留 `temp/cloudbridge/`（不进 git）。同日：AIGC 能力盘点——`C:/Kimi Code/aigc-guides`（dreamina/mmx CLI 手册仓）指针进 AGENTS 环境备忘。
- **2026-08-09｜展品 004「大哥大」：img2threejs 首件 AI 重建展品，8 pass 质量门全过（Playwright 双端 24 断言全过）**：从「找仓库」到「进馆」全链——克隆 `img2threejs/img2threejs` v1.4.3（钉 annotated tag，GitHub API 校验 commit `acd252c`）到 `../tools/img2threejs`；**Kimi 适配层**落 `~/.agents/skills/img2threejs/SKILL.md`：便携 Python（`../tools/python-embed`，embeddable zip 零系统污染；`._pth` 隔离吃 PYTHONPATH/脚本目录，stage3_build/stage4_review/stage1_intake/stage2_spec/forge/_shared 五个目录要写进 `python312._pth`）+ ffmpeg 转 8-bit PNG（脚本 sips 回退是 macOS 专属）+ Playwright 截图评审。**参考图 = dreamina 生成**（4.0×3 + 5.0Pro×3 六候选用户挑定 5.0Pro 直立棚拍款——听筒打孔/侧键/磨砂纹理全清晰，单物体浅灰底无字）。**spec 手工深化**（`../tools/i2t-runs/dynatac/deepen_spec.py` 幂等可重跑）：26 组件/8 材质/3 重复系统/11 细节清单条目，strict-quality PASS。**8 pass 评审闭环**：生成器骨架 → `refine_ts.py` 精修层（同锚点幂等）→ exhibits `?review=` 评审页截图 → `diagnose_render` Tier1 → `make_comparison_sheet` → 人眼五层记分卡 → `append_review` → sync。**关键坑（全记 PITFALLS）**：① 生成器把子件挂进带缩放的父节点（root scale 2.7 时天线飞出两倍）——**root scale 必须 [1,1,1]，尺寸烘焙进几何**；② 评审页 OrbitControls 每帧 update 把相机 lookAt 回 (0,0,0)——target 要同步到包围盒中心，roll 同理只能滚模型；③ Tier1 剪影 IoU 是 224² 全图逐像素比对，渲染取景必须在参考图同一比例/位置（az/el/margin/cy 扫参法定到 az44/el10/margin1.25/cy-0.05，IoU 0.839→0.854 过线）；④ **Tier1 色彩门对小面积饱和点缀色系统性误报**（kmeans k=5 对 <1% 像素不成簇 + ACES 渲染偏暗 20-40，红黄绿三键 deltaE 63）——按透明原则记录为 harness 局限并继续；该门同时揪出真 bug（fn 键 recipe 全写红）。**精修亮点**：听筒 superellipse 圆角方形环/盘替换生成器椭圆+圆环；蜂窝孔阵手写 InstancedMesh（生成器 radial 环在父缩放系错位，移除）；键帽白色字符 canvas 贴 +Z 面；合模缝/斜切凹槽/下巴棱线几何件。**进馆**：工厂 `exhibits/src/models/createBrickPhoneModel.ts`（92k tris/182 draw calls，预算内）+ `brickphone.vue`（reference 灯位+环境贴图+转台 90s/圈+软接触影，`window.__stage` 探针）+ manifest 一条；封面 dreamina 四候选（油画静物/复古海报/像素/铜版）用户选定**油画静物**（伦勃朗式暖光木桌静物），128KB 入 `public/covers/`。大厅四画轮播循环/点进展品/题签/返回，双端 24 断言全过。落选素材在 `../tools/i2t-runs/dynatac/{ref,cover}/`，流水线档案（spec/reviewHistory/截图/对比图）同目录留存。
- **2026-08-09｜展览馆大厅第二轮改版：极简虚空 + 黑镜倒影 + 单灯追画（Playwright 双端 26 断言全过）**：首轮「AI 房间背景 + 画框」被用户 review 指出两个别扭——背景轮换换墙不动画、烤进图里的射灯与 DOM 画框是两套坐标系永远对不齐；定调「灯归画框管，背景只管氛围」，背景题材进一步从「房间」改为**纯 CSS 渐变虚空**（零图片资产，`public/bg/` 与 `LOBBY_BGS` 轮播逻辑整体删除；3 张 AI 房间图留 `temp/gallery-gen/` 备用）。三层结构：虚空（radial-gradient）→ 灯（**只给选中的画**：顶部光锥 + 框周溢光 box-shadow + 地面光池，`.frame.lit` 切换，随选中态 0.5s 过渡）→ 画（轮播主体，侧画 brightness 0.62→0.5 隐入暗处）。**黑镜倒影** = 封面翻转克隆（`scaleY(-1)` + 渐隐蒙版 + blur 1px + opacity 0.45/蒙版首段 0.75α）作为画框子元素——天然跟随轮播变换，且侧画倒影自动被 filter 压暗；弃用 `-webkit-box-reflect`（Firefox 不支持，且按未变换 border box 定位，scale 下倒影与画脱钩）。**坑**：`clip-path` 在渲染管线中位于 `filter` 之后，同元素的 blur 会被裁成硬边——模糊挂外层容器、`clip-path` 挂内层 `<i>`（已记 PITFALLS）。截图迭代三轮：① 光锥 150% 宽+blur 7px 太宽太硬像灰帐篷 → 120%+blur 14px+透明区拉长到 55%；② 倒影/光池太淡 → 蒙版首段 0.5→0.75α、光池 0.2→0.3α。验收：双端 13×2 断言（三画框/三倒影/亮灯画仅一幅/无背景层/封面加载/零文字/箭头/灯跟画走/键盘/点侧画/拖拽/点中画进展品/返回大厅）。
- **2026-08-09｜展览馆大厅改版：无字画框轮播 + AI 生成背景/封面（Playwright 双端 20 断言全过）**：方向对齐（用户拍板，参考「虚拟画廊」图）——每展品一幅 AI 封面画装进 CSS 复古相框，Cover Flow 左右滑动/点选切换、点中间画进入；**进作品前全馆零文字**（馆头/编号/简介/「共 N 件」全删，manifest 的 title/desc 降为存档字段，保留 alt/aria）；封面静态画 ↔ 进去动态特效的反差即策展语言，封面画风可自成一派。素材：dreamina CLI（Seedream 4.0，`~/bin/dreamina.exe`，提示词按 `aigc-guides/prompt-writing.md` 模板）出 15 候选——3 背景 + 3 展品 × 4 画风；用户选定：背景**三张全留做轮播**（墨蓝丝绒墙/胡桃木墙板/水泥灰，ffmpeg 1920px `-q:v 3` 各 ~150–215KB）、封面 = 梵高厚涂流星雨/铜版星图地球/岩彩金箔银河（1024px `-q:v 3`）。落选 12 张候选在 `temp/gallery-gen/`（本地未进 git，可作未来换图素材）。实现：`App.vue` 大厅重写——`LOBBY_BGS` 列表多张自动 9s 淡入淡出 + Ken Burns 26s 缓推（换图 = 图丢 `public/bg/` + 改列表一行）；画框纯 CSS（深木外框 + 做旧金线 + 卡纸内衬，宽 `min(46vw,52vh)`）；轮播 = 环形 offset（0/±1）+ `translateX(off×38vmin)` + `rotateY(-off×14°)` + scale 1/0.62 + brightness 0.55，zIndex 递减；拖拽 Pointer Events（位移 >60px 切画、<8px 算点按）+ 键盘 ←→/Enter；manifest 每条加 `cover` 字段（`public/covers/` 4:3）；返回按钮改纯 SVG 图标（aria-label 保留）。**坑**：墙容器 pointerdown 即 `setPointerCapture`，pointerup 的 target 恒为容器自身，`e.target.closest` 拿不到画框——改 `document.elementFromPoint` 解析命中（已记 PITFALLS）。验收：双端 10×2 断言（三画框/三背景/封面加载成功/大厅零文字/箭头切换/键盘切换/点侧画选中/拖拽切换/点中画进展品/返回大厅）+ 双端及桌面壳集成截图人工把关通过。
- **2026-08-08｜新歌二连：随身听上架《孟婆》《七情六欲》（Playwright 双端 46 断言全过）**：源文件来自 `V/` 私人空间（256k 原档）；封面为即梦 Seedream 新出图（水墨极简古风，每歌 4.0/5.0Pro 双候选，用户选定《孟婆》= 5.0Pro 单株彼岸花、《七情六欲》= 4.0 墨人背影，1024² 原图收进 `V/`）。ffmpeg 既定通道：mp3 `-b:a 128k -ar 44100`（孟婆 8.3MB→4.2MB/260.3s、七情六欲 7.6MB→3.8MB/238.1s），封面 512px `-q:v 4`（25KB/21KB）；波形 = ffmpeg s16le 管道 + 一次性 Node 脚本（N=112 RMS，同 07-26）。`WIN98_TAPES` 注册两条，**取色首次改为脚本采样**：孟婆 #db170b（全图「饱和度×明度」最大像素，彼岸花朱红）、七情六欲 #64625c（亮度 0.15–0.62 区间墨像素均值，暖墨灰），现 **7 盘磁带**。验收：playwright-core + chromium-1234，双端（1280×800 / 390×844）23×2 断言——架头 `TAPES · 7`、新歌居第 6/7 位、架显 4:20/3:58、四资源 200、点带入舱 deck/busy/`--accent`/dataset.cur/tp-cap/封面 naturalWidth、入舱自动播放、零 pageerror；双端截图人工把关通过。
- **2026-08-07｜展品 002/003 拟真化调优：节奏与形态按真实宇宙校准**：用户反馈地球「自转/阴影速度不协调」、银河「转太快」，定调「贴近真实的宇宙，可带创作」。**002 蓝色弹珠**：自转 90s→**300s/圈**（一天压成 5 分钟）；云层差速 **×1.35→×1.05**——真实大气与地表基本共转，旧值让云在陆地上肉眼可见地滑动，正是「不协调」主因；镜头 autoRotate 0.35→0.2。结论备忘：**不加真实光源/月球**——自定义 ShaderMaterial 不吃场景光（太阳只是方向 uniform，加 DirectionalLight 无视觉效果），且按真实比例公转一圈 300s×365≈30h、月球一圈 ≈27 天，展品时间尺度下均不可察觉。**003 亿万星尘**：形态按真实星系重做——4 条等距对称臂（真实仅占 ~2%）→ **2 主旋臂 + 棒状核心 + 核球**（约 83% 类银河系为「内 2 主臂 + 外不规则臂段」，约 2/3 旋涡星系带棒，银河系即棒旋；依据中科院紫金山天文台徐烨团队 2023 研究，央视/新华社报道）；randomness 0.38→0.5 / power 3.2→3.0 模拟絮状臂段，银盘压扁 ×0.5→**×0.32**（真实星盘很薄）；核球 10%（半径 0.2R、点尺寸下调）+ 棒 8%（长 0.38R 椭球，端部收细，与两臂起点 0/π 对齐）；色温微调内暖外冷（#ffb27d/#6b8fe6/#ffe9c4）；旋转 0.05 rad/s→**2π/420s 约 7 分钟一圈**（真实星系一圈数亿年，观感「几乎静止」才对）；镜头 0.25→0.1。**审美坑（首轮截图抓出）**：核球 15%+棒 10% 叠加 additive 与 bloom，核心区过曝成大白团——回调密度/尺寸 + bloom 0.38→0.32、threshold 0.2→0.25 后出片。验收：Playwright CLI 截图双端（1280×800 / 390×844）两轮人工把关；移动端 50k 粒子无 bloom，棒形仍清晰。

- **2026-08-07｜展品 002「蓝色弹珠」+ 003「亿万星尘」：three.js 进展馆（Playwright 双端 18 断言全过）**：展柜首个 WebGL 展品线，借鉴成熟实现「站肩膀」（调研纪要与出处见下）。**工程**：`three@0.185.1` 进 dependencies；新增共用舞台封装 `src/lib/threeStage.js`（renderer/ACES tone mapping/DPR 上限 PC2 移动1.5/OrbitControls/resize/帧循环/dispose；`texURL()` 取 `public/textures/` 贴图，base './' 通用）；新展品照旧只加 `src/exhibits/xxx.vue` + manifest 一条，壳零改动；three 打进独立 chunk（544KB min/136KB gzip），仅两展品异步加载，大厅与流星雨不吃。**002 地球**：自定义 ShaderMaterial 昼夜混合（`smoothstep(-0.12,0.12,sunDot)` 晨昏线 + 暮光橙带，官方 tsl_earth 范式；sunDirection 世界空间、shader 内乘 viewMatrix 转视线空间——viewMatrix 在 fragment 是 three 内置 uniform）+ fresnel 边缘蓝雾 + fresnel 大气辉光背面球（1.06x，BackSide+Additive，`pow(0.62-dot(n,(0,0,1)),2)`，昼侧增强）+ 云层独立球差速自转（×1.35）+ 地轴 23.4° 父组倾斜。贴图 three-globe（unpkg 直下，MIT）+ SSS 云（CC BY 4.0），ffmpeg 统一压到 2048px q4/5 共 ~700KB。**003 银河**：Galaxy Generator 算法（Bruno Simon Three.js Journey：4 臂、spin 1.15、randomness 0.38^3.2、`pow(random,2)` 密度收核心、12% 粒子高斯核心 bulge、银盘 y×0.5 压扁、内 #ff9e5e 外 #4a72d4 按 `pow(r/R,0.8)` 渐变）+ 自定义 shader 软圆点（`gl_PointCoord` 径向衰减 discard，尺寸随透视衰减）+ PC 端 UnrealBloomPass（半分辨率，strength 0.38/radius 0.35/threshold 0.2 + OutputPass），移动端关 bloom 且粒子 200k→50k。**审美调参两轮（截图人工把关）**：① 银河 core 过曝成白团——bloom 0.55→0.38、threshold 0.12→0.2、uSize 26→22、曝光 1.15→1.0；② 地球夜侧占画面过大死黑——太阳方向 (4,1.2,2.5)→(2.5,1.4,4.2)、夜光 ×1.4→1.8、加 0.02 环境底光；③ 关键一步：昼贴图从 blue-marble（海洋近黑）换 three-globe `earth-day.jpg`（明亮经典款）+ day×1.15 + 曝光 1.2，湛蓝地球出片。**坑**：npmmirror 连发 502/503（tarball 下载抽风），换 `https://mirrors.cloud.tencent.com/npm/` 4 秒装好——本机 npm 镜像优先级：腾讯 > 阿里 > 默认；无头 Chromium 跑 WebGL 需 `--use-gl=angle --enable-unsafe-swiftshader` 兜底（本机实跑渲染正常）。

- **2026-08-07｜新歌二连：随身听上架《Moonlit Tide》《Rainy Morning》（Playwright 双端 30 断言全过）**：源文件来自 `V/` 私人空间（moonlit-tide / rainy-morning，256k 原档），用户拍板歌名沿用英文（现有三盘中文名之外的首次英文命名）。ffmpeg 主力通道全复用：mp3 `-b:a 128k -ar 44100`（Moonlit Tide 5.3MB→2.6MB/166.5s、Rainy Morning 5.1MB→2.5MB/159.7s），封面长边 512px `-q:v 4`（24KB/42KB）；波形 = ffmpeg 解码 s16le mono 44100 管道进一次性 Node 脚本（N=112 分桶 RMS、`0.12+0.88·n^0.7`，算法同 07-26）。`WIN98_TAPES` 注册两条（Moonlit Tide #dfa63e 月金 / Rainy Morning #8fa08a 雨晨灰绿，取色自封面），现 **5 盘磁带**，MEDIA_CDN 保持 ''。验收：playwright-core 1.62.1 + 复用本机已装 Chromium（`~/AppData/Local/ms-playwright/chromium-1234`，注意二进制在 `chrome-win64/` 子目录，旧脚本找 `chrome-win` 会扑空）；双端（1280×800 / 390×844）15×2 断言：架头 `TAPES · 5`、新歌在第 4/5 位、时长 2:46/2:39、点带入舱 deck/LCD 标题/`--accent`/封面 naturalWidth/音频 dataset.cur、入舱自动播放。坑一记：npm 默认 registry 本机卡死，`--registry=https://registry.npmmirror.com` 秒装（与 github 走 gh-proxy/SSH-443 同一网络体质）；`WIN98_DESKTOP.openModule` 收的是**模块配置对象**不是 id 字符串（`WIN98_MODULES.find(m => m.id === 'tape')`）。

- **2026-08-07｜ICP 备案号上任务栏托盘（腾讯云备案已通过）**：备案号 `粤ICP备2026111478号` 落定，公安备案仍在审批（审批下来后需再加公安备案号 + 查询页链接）。实现：`index.html` 任务栏托盘（`#taskbar-tray`）时钟左侧加 `<a id="beian-link">` 指向 `https://beian.miit.gov.cn/`（`target="_blank" rel="noopener noreferrer"`）；`style.css` 托盘加 `gap: 8px`，链接样式 11px/#222/默认无下划线 hover 补下划线。取舍：全站无传统页脚，任务栏托盘 = 唯一常驻底部区域，备案号放这里既满足「首页底部可见」又不侵占桌面主体；移动端不隐藏（合规优先，任务按钮区自有横向滚动兜底）。未动 exhibits 子站（Vue 构建产物，且备案检查以首页为准）。本机 Playwright 不在（验收脚本一次性铁律产物），仅做静态验证：8098 本地预览 curl 确认链接与样式 200 渲染，起服务前查端口、杀服务后验端口（8098 已释放）。视觉终验待部署后用户确认。

- **2026-07-27｜屏保触发时间 60s → 5 分钟**：`js/screensaver.js` `IDLE_MS` 改为 `5 * 60 * 1000`（用户要求，60s 触发太频繁）；文件头注释与 AGENTS 架构表同步；巡检间隔/宽限期/展品 URL 不动。

- **2026-07-26｜新歌二连：随身听上架《夜晚》《应龙》（Playwright 双端 32 断言全过）**：加歌流程首次用 **ffmpeg 主力通道**（`../tools/ffmpeg/node_modules/ffmpeg-static/ffmpeg.exe`，AGENTS 环境备忘既定）：mp3 `-b:a 128k -ar 44100`（夜晚 5.5MB→2.75MB/172.1s、应龙 7.6MB→3.8MB/237.8s），封面 `scale=512:-1`(按长边)`-q:v 4`（≤21KB）；**波形也走 ffmpeg**——解码 s16le mono 44100 管道进一次性 Node 脚本，复刻 `waveform-extractor.html` 算法（N=112 分桶 RMS、max 归一、`0.12+0.88·n^0.7`），不再依赖浏览器工具。`WIN98_TAPES` 注册两条（夜晚 #8f7fd4 夜紫 / 应龙 #6fae9f 雾青，取色自封面），MEDIA_CDN 保持 '' 无需动。验收探针：`bodyEl.win98Tape.deck` / `win98TapeAudio.dataset.cur` + `--accent`。双端（1280×800 / 390×844）断言：架头 `TAPES · 3`、三盘齐全、时长 2:52/3:57、点带入舱 deck/accent/audio src 正确、四资源 200。收尾复演经典坑：TaskStop 只杀掉 npx 壳，8098 仍被 npx 子进程（PID 39640）占着，按收尾清单 netstat 验证后补 taskkill——**npx 服务杀完必须验端口，铁律又中一次**。
- **2026-07-26｜inspira-ui 镜像在 Windows 机就位**：`git clone --depth 1 https://gh-proxy.com/https://github.com/unovue/inspira-ui.git ../tools/inspira-ui`（11MB，github 直连不稳走 gh-proxy 一次过），组件源路径 `app/components/inspira/ui/<组件名>/` 与 Mac 一致，双机挑展品组件能力对齐；gh-proxy 同时明确为本机拉取 github 公开资源的**通用备用通道**（COLLAB 在役机器表已注）。
- **2026-07-26｜本机 ffmpeg 就位**：`cd ../tools/ffmpeg && npm i ffmpeg-static`（postinstall 从 GitHub releases 拉 ~77MB 二进制，本次网络放行）→ **ffmpeg 6.1.1**（gyan essentials 静态版，含 libmp3lame/libopus/x264/x265），版本 + 解码冒烟均过。定位：本机音视频处理主力（转码/压缩/批量/探测），二进制 `../tools/ffmpeg/node_modules/ffmpeg-static/ffmpeg.exe`（长期资产，已列入收尾清单保留项与 COLLAB 按需安装表）；浏览器工具（audio-optimizer / waveform-extractor）仍是双机零安装兜底。注意仓库本体不放 ffmpeg（零构建纯静态 + 二进制体积）。
- **2026-07-26｜卡顿二修：CDN 证伪全灭，改「压缩素材 + 本地直供」**：镜像方案上线即被用户实测打脸——深挖发现 **jsDelivr /gh/ 对 mp3 不做缓存**，301 甩给 raw.githubusercontent.com（该域名国内极不稳：同一 URL 前次测 1.8MB/s、复测 335B/s），statically.io / raw.githack.com 本网络不可达——外部 CDN 通道全灭；唯一稳定通道 = github.io 自身（Fastly HIT ~24KB/s）。新策略 = 把素材压到该通道供得起的码率：新增零依赖工具 `tools/audio-optimizer.html`（内联 lamejs（LGPL，仅开发工具不进运行代码）MP3 编码 + canvas 封面压缩），《月之暗面》7.9MB/256k → **3.76MB/128k**（16KB/s，通道留 1.5 倍余量）、封面 565KB → **62KB/512px**（时长不变，波形数据复用）；`MEDIA_CDN` 置 '' 全回本地（机制与注释保留，未来发现可用镜像填回即用）；预载 + LCD 缓冲指示保留。教训：①「curl 一次快」≠「浏览器一直快」——CDN 类方案要测**最终数据落点**且多时点多测；② error 回退只兜「硬失败」兜不住「慢而不死」，通道选型看稳态吞吐不看峰值。原 256k 曲在 git 历史（3e3878a）可恢复。加歌流程同步改为：丢源文件 → audio-optimizer 压缩 → 波形提取 → 注册。
- **2026-07-25｜随身听卡顿定修：jsDelivr 镜像 + 钉 commit 防 301 抖动（双探针 10 断言全过）**：用户线上实测播放卡顿。实测定因（`curl -w "%{speed_download}"` 对比码率）：github.io 直连吞吐仅 **23.7KB/s**，歌曲 256kbps 需稳定 32KB/s——**纯网络问题，非代码问题**（波形内联 JS 零请求、封面 565KB 同通道也慢）；jsDelivr 同文件实测 **1.79MB/s**（75 倍）。修复（`js/apps/tapeplayer.js` 顶部 `MEDIA_CDN` 常量）：音频/封面 src 走 jsDelivr 镜像 + error 自动回退站点本地路径 + 开窗即预载首盘（原来入带才加载）+ `waiting`/`stalled` 时 LCD 显 `LOAD`（canplay 恢复时间码）。**关键坑：jsDelivr 分支地址（@main）会 301 跳转且落点不稳定**（curl 快、Chromium 偶发立即 error 触发回退），钉 commit hash 后直出 200/206 稳定且永久缓存免 purge（已记 PITFALLS）。验证：CDN 路径 5 断言全过（buffered>100s、2s 连续播放无停顿）、CDN 故障回退路径断言全过。注意：以后 push 新歌/新封面要同步 `MEDIA_CDN` 的 hash（已写进 AGENTS 环境备忘与模块注释）。【后续：次日被用户实测证伪，镜像对 mp3 实为 301 到 raw.githubusercontent 而非真缓存，见上一条 07-26】
- **2026-07-25｜新模块「卡带随身听」上线（A 级，Playwright 双端 14 断言全过）**：复古壳 × 现代芯的新作品容器——每首歌 = 一盘磁带，点磁带飞行入舱（FLIP 动画）→ 相框封面 + 真实波形出现 → 自动播放。结构：`js/apps/tapeplayer.js` 单文件模块（数据注册表 `WIN98_TAPES` 在头部，加歌 = 丢文件进 `assets/music/` + `tools/waveform-extractor.html` 抄 duration/peaks + 注册表加一条）+ `.app-tape` 样式进 style.css + config.js 注册（970×480）+ tape.png 像素图标（24×20 字符矩阵，`make_icons.py` 与一次性 Node 脚本双实现同源——本机无 Python/Pillow）。视觉（经 4 轮原型评审迭代）：窗口内整片深色 #16161c（壳守 98 芯自由），机身 = **斜二轴测 2.5D**（顶面散热槽 + 右暗面 + 正面阶梯圆角 + 按键投影；鼠标视差方案废弃——静态挤出才有体积感，视差没用；首版灰盒被用户吐槽「像微波炉」故推倒），主题色随磁带变色（色条/播放环/LCD/磁带标签/波形已播段/氛围光晕统一走 `--accent` CSS 变量）。交互：⏮⏭ 短按换带（自动退带 + 入带）/ 按住 ≈8x 快进快退（卷轴狂转）；波形点击/拖动 seek；音量滑杆（iOS 系统忽略 audio.volume 仅 UI 反馈）；空格播放暂停（仅窗口聚焦时）；←→ ±5s；唱完自动换带；关窗自动停声（rAF 检测 isConnected，同 about 星空模式）。**废弃项**：机械音效（Web Audio 合成咔哒/入带声）——用户评审「不拟真且与音乐混叠」；下载的 3 首 Kevin MacLeod + Picsum 封面——用户决定只保留原创曲，以后加歌再走注册表流程。波形提取器：OfflineAudioContext 解码分桶 RMS（ffmpeg-static 无 bin 被 npx 拒、postinstall 走 github 不稳，已记 PITFALLS）。修：tap 判定处理器漏写 `(e)` 形参致触屏播放键全灭（PC 用例恰好没点过该键漏网，`pageerror` 监听定位，已记 PITFALLS）。默认曲《月之暗面》（用户原创，246.6s）。
- **2026-07-25｜记忆碎片定性 = 1998 年的老物件，双结局定型**（Playwright 双端 30 断言全过）：碎片从抽象计数具象成**老物件收集**——`FRAG_OBJECTS` 22 件（3.5 英寸软盘、1998 年电影票根、小浣熊水浒卡、电子宠物蛋、《泰坦尼克号》VCD……），**22 = 理论最大获得数**（B1–B10 全清各 +1 共 10、每层宝箱 35% 共 11、B11 全清 +1）；newRun 时 Fisher-Yates 洗牌本局物件池，`gainFragment()` pop 一件**不重复**，全清/宝箱掉碎片时 toast 直接报物件名（「全清奖励：捡到一张还珠格格贴纸」）。结局按用户定调重写：**成功** = 回信「未来的你，还好吗？……如果累了，就回来吧。重新当一个会为一张票根、一片树叶发呆半天的少年。—— 1998 年的你」（记事本样式保留）+「你带回的记忆：」晒出本局拾得清单；**失败** = 不再出示残缺信（LETTER_BAD 删除），直给「硬盘坏道已经无法修复，信件没有找到。请重新开始。」+「你只捡回了：……它们还拼不出那封信。」STORY_INTRO/HUD title 同步「每件碎片都是 1998 年的老物件」。实现细节：gainFragment 用 `fragments += 1` 而非从 fragFound 派生（外部直接改 fragments 时不脱钩）；结局物件清单行 = `.mine-dg-story` 复用样式，无新 CSS。报物件名的 toast 加长到 2800ms（`PICK_TOAST_MS`，默认 1200ms 读不完）、全清自动下楼延时同步放宽到 1800ms（`CLEAR_DESCEND_MS`，防止覆盖层提前盖住 toast——实测用户反馈读不完后调整）。
- **2026-07-25｜收尾流程堵漏（流程类，无代码改动）**：这轮地下城验收暴露两个收尾漏洞——① 起本地预览时发现 **8098 已被占用**：一查是上次（或更早）收尾漏杀的 `npx http-server -p 8098 -s` 残留（npx 缓存路径，PID 34908），已 `taskkill` 并验证端口释放；② 想复用上轮验收脚本找不到——验收脚本写在仓库外 `../tools/`、收尾即删本是铁律 9 的既定设计，但文档只说了「删」没说「一次性、别找旧的」。处理：AGENTS 铁律 9 从一句话扩成独立「**收尾清单**」小节，三项逐项打钩——杀临时服务**必须验证端口空了**（Windows `netstat -ano | grep 8098` / Mac `lsof -i :8098`）、删 `../tools/` 本次产物（保留 inspira-ui 与 gh CLI 长期资产）、`git status` 只剩预期改动 + 文档已同步；环境备忘补两条——验收脚本一次性（现写现删，历史断言结论翻 DEVLOG，状态读取走 `win98Board`/`win98DgRun`）、Windows 预览命令补 `-c-1` 禁缓存并要求**起服务前先查端口**。
- **2026-07-25｜地下城四连修 + 双结局文案重写**（Playwright 双端 24 断言全过）：① **踩雷后左上计数器不减**——根因有两层：`remainingFlags()` 只减插旗和 known、没减 exploded（已爆雷仍占「未发现」名额）；更深一层是 core 先调 `onMineHit` 钩子（模式方在钩子里 refresh HUD）、返回 false 后才置 exploded，钩子里算出来的永远是爆前状态。修复：remainingFlags 计入 exploded（与 flagged/known 互斥），core 改为**钩子前预置** exploded/revealed、致命再收回走 lose（钩子契约注释同步）。② **声呐脉冲/探测仪会浪费在已插旗的雷上**：`markRandomMines` 与 dungeon `unmarkedMines()` 的池子都不排除 flagged，现统一口径——已插旗=已发现，不再占可标记名额。③ HUD 金币/碎片图标加文字标签（`金币 0` / `碎片 0`，碎片图标 ◆ 字符→diamond-gem 像素 svg 新配色 px-frag）；碎片**不再显示 0/5**（超过目标后 x/5 没意义），目标数挪进 title 提示「集满 5 片可修复（当前 n/5）」，集满追加「· 已集满」；结局统计同步改为「还差 n 片 / 已集满」。④ B11 提示重写并讲清规则「这层没有出口，全清所有安全格才能挖出时间胶囊，坏道反扑踩雷 -2」——文案长，toast 加了可选时长参数（默认 1200ms，这条 4200ms）。⑤ 双结局信件重写：好结局完整信加细节（磁头走路声/扫雷通关截图/没敢寄出的信，落点「好奇心不会」），坏结局保持同一封信挖 □ 的对位结构，收尾文案改为「坏道吞掉了太多数据……凑齐碎片把它修得更完整」。**验收坑**：经典与地下城面板的 LCD 都叫 `[data-role="mines"]`，验收脚本 `querySelector` 会命中经典那个，读数永远不动，误判产品 bug——选择器要加 `.app-mine-dg` 作用域（已记 PITFALLS）。
- **2026-07-25｜存量 emoji 全量像素化（三模块并行改造，合流双端 11 断言全过）**：试点推广到全部模块，JS 运行代码 emoji 清零（数据层保留 ♠♥♦♣ 花色字符与 ◆，仅作数据不作渲染）。统一模式：各模块局部 `PX` 表 + `px(name)` helper（pixelarticons@2.2.0 取 path 内联 SVG，MIT；库没有的图标按 2px 方块风格手绘 24×24 path）。**地下城**（45 处）：HUD 桃心/金币、棋盘装饰（出口洞/爆炸星/雷达标 = 手绘 hole/boom + signal）、结局胶囊、toast 文案 13 处去 emoji 留文字；px() 加第二参数产语义色 class。**扑克**（40 处）：顶栏 💡🔊📊📖🔄、头像 🤖😎、筹码 🪙、胜负标题→像素 svg；牌面花色 SUITS 数据结构不动，渲染走 `SUIT_D` 自绘矢量 + `suitIcon()`/`suitify()`（日志新增 logHtml 通道，战绩存储格式不变兼容旧 localStorage）；音量 🔊/🔇 双态。**经典扫雷**（13 处）：插旗 🚩→像素旗（黑杆红旗分段 fill）、💣→手绘像素雷（黑球+八向刺+高光）、❌→像素叉、笑脸四态 🙂😮😵😎→手绘黄脸像素 svg（CORE.px 导出供容器复用，钩子接口零改动）。**复用教训**：svg 内 icon 一律显式 fill 分段写色（98.css 按钮透明字坑）；CSS 分作用域（`.app-mine svg.px-core` / `.app-mine-dg svg.px-icon` / `.app-poker`）互不干扰。注意：`.app-poker` 两处 CSS content 装饰花色字形未动（不在 DOM）。
- **2026-07-25｜壳/芯风格边界确立（THEME v1.4）+ 地下城图标像素化试点**：用户定调——**最低限度是桌面图标风格统一（自绘像素风），窗口内部完全自由**、不被 98 束缚（THEME 铁律改写、PLAN 原则 5、AGENTS 铁律 3 同步松绑）。图标库选型落地：**pixelarticons（MIT，877 枚 24×24 像素 SVG）**，不引依赖、只取 path 内联为 JS 字符串（mine-dungeon.js 的 `PX` 表 + `px()` helper，`fill=currentColor`），替代 emoji——地下城职业/技能/装备/消耗品/三选一/商店卡片的 17 处图标全部换像素 SVG（数据层替换，职业选择/装备栏/奖励/商店同步生效），双端 Playwright 断言全过（卡片 3 图标、装备栏 ≥4 图标、无控制台错误）。**坑：98.css 按钮文字是 `color:transparent` + `text-shadow` 画的，按钮内 svg 用 currentColor 会渲染成全透明**（修复=按钮内 svg 显式 fill，已记 PITFALLS）。存量 emoji：mine-dungeon 62→45（剩余为 toast 文案/棋盘装饰/HP 桃心，后续顺带替换）。
- **2026-07-25｜文档体系重构**：AGENTS.md（22KB）与 PROJECT_PLAN.md（20KB）双份日志合并拆出本文件 + `PITFALLS.md` 踩坑录，AGENTS 瘦身为 30 秒锚点（当前阶段只留近三轮）；确立**多端适配 A/B/C 分级**（A=双端完整 / B=手机可用可简化 / C=PC 优先，定义见 PROJECT_PLAN.md §5.4，config.js 逐模块标注），新模块注册必须定级、按级别验收；确立 **emoji 图标禁令**（跨端渲染差异大，UI 图标一律自绘像素 PNG，存量 115 处登记在 PITFALLS.md 逐步替换）。同日本机 Playwright 浏览器升级到与 latest CLI 配套（chromium-1234）。
- **2026-07-24｜安卓幽灵 click 根治**（Playwright 三端 23 项断言全过）：用户实测 iOS 已正常，但安卓扫雷开局后难度按钮/模式 Tab 全部「失灵」（最小化/最大化/关闭正常），另报告关窗重进地下城「进度还保留着」。Playwright 安卓触屏仿真复现 + 事件探针定位，根因有二：① Android Chrome 的 pointerdown `preventDefault()` 抑制不住原生 click（此前注释「Android Chrome 生效」的假设错误），每次点按 = 补发 click + 原生 click 双发；② 防误触确认框一打开就给 `.app-mine` 上 `inert`，原按钮被移出命中测试，迟到的原生 click 被重定向到 `.mine-dialog-overlay` 遮罩，「同控件 700ms」去重比对不上 → 漏吞 → 遮罩把幽灵 click 当「点空白取消」把框瞬间关掉——用户看到的就是开局后按钮全死（未开局不弹确认框所以正常；标题栏三键不触发确认框不受影响；iOS 原生 click 本就不派发所以没事）。修复：`js/touchTap.js` 去重改为 **`isTrusted` 判定**——补发的 `el.click()` 是不可信事件永远放行；补发后 700ms 内到达的可信 click 一律吞掉（不再比对命中目标，DOM/inert 重定向也兜得住）；任意新 pointerdown 作废旧记录，不误吞后续真实点按；附带修好旧去重「700ms 内同控件连点第二下被误吞」。**「进度保留」排查结论：代码无此 bug**——关窗即销毁 DOM，重开必然重新渲染（run=null 回职业选择，Playwright 实证）；用户所见应为误点最小化（任务栏还原保留进度是设计行为）或坏状态下未能真正关窗。回归：安卓触屏 15 项 + PC 鼠标 4 项 + iOS WebKit 4 项全过；真机需用户终验。
- **2026-07-24｜iOS 真机 click 失效根治**（Playwright 34 项断言三端全过）：iPhone 真机复测发现窗口内仍然「全部点不动」——格子轻点不翻开（但按下变脸 😮 正常）、难度/Tab/标题栏三键无反应，只有长按插旗（pointerdown+计时器）能用。诊断：窗口内 Pointer Events 正常而 **click 事件整个不派发**（凡是 click 驱动的全灭、pointer/timer 驱动的全活；桌面图标能用只是因为早走了 pointerup 逻辑），疑似 WebKit 点击命中测试与 DOM 事件不一致的祖传怪癖（与 `#windows` 曾设 `pointer-events:none` 同源），无头 WebKit 无法复现。修复 = 绕开系统合成 click：新增 `js/touchTap.js`——触屏 pointerdown 记录并 `preventDefault()`，pointerup 校验时长≤600ms/位移≤12px/起落同控件后直接 `el.click()` 补发（全站 `button`、开始菜单 `li`、`#shutdown-overlay`；`.click()` 走完整冒泡链，委托与 onclick 均覆盖），个别 preventDefault 不生效的平台由捕获阶段去重保证只激活一次；鼠标路径（pointerType=mouse）完全不动。配套：`mine-core.js` 长按插旗的 `suppressClick` 布尔改为 `lastFlagTime` 时间戳判定（布尔法在补发 click 路径下会残留并误吞下一次轻点）；`taskbar.js` 开始菜单补 pointerup 外点收起（iOS 下点桌面空白也能收）。**注意：TAP_MS 600 必须 > 扫雷长按判定 450ms；长按后抬手的补发 click 由 lastFlagTime 700ms 窗口挡住，不会误翻开**。真机需用户终验。
- **2026-07-24｜iOS 真机触控修复**：用户反馈 iOS 上打开窗口后标题栏按钮/窗口内容全部点不动。根因：iOS Safari 下 `#windows` 的 `pointer-events:none` 会让子元素即使声明 `pointer-events:auto` 也接收不到触摸；现 `windowManager.js` 动态控制 `#windows` 的 `pointer-events`——存在最大化窗口时设为 `auto`（保证手机全屏窗口内按钮可点），其余情况恢复 `none`（不挡桌面）。同时修复 `@media (max-width:640px)` 把标题栏按钮热区意外覆盖回 20×18 的问题；`desktop.js` 的 `suppressNextClick()` 增加 `touchstart` 撤防，进一步降低误吞真实点击的风险。PC/手机 WebKit Playwright 回归测试通过，真机需复测。
- **2026-07-23｜阶段质检三端过检**（PC/安卓 Chromium + 苹果 WebKit 双引擎 Playwright 88 项断言全过，真机复测待用户确认）：① 修触屏开窗串扰（苹果端"功能失效"主嫌）——图标第二击在 pointerup 同步开窗，浏览器随后派发的合成 click 落在图标原屏幕位置（已是新窗口内容），双触扫雷会直接点进「寻找时间胶囊」Tab/误点格子；现 `desktop.js` 开窗即 `suppressNextClick()` 吞掉紧随的一次合成 click（capture 拦截；真实新点击从 pointerdown 开始先撤防再放行，500ms 兜底）② 触屏打开图标放宽：点选后再点一次即开，去掉 450ms 限时（手机上双击节奏不可靠，限时=怎么点都打不开）③ iOS 适配加固：viewport 加 `viewport-fit=cover` + 任务栏/桌面/开始菜单计入 `env(safe-area-inset-*)`（开始按钮不再被 home 指示条压住）；**窗口层 `#windows` 四向都让出 safe-area——最大化窗口标题栏按钮若陷进刘海/状态栏区域，iOS 会把那里的触摸拦截给系统**；触屏标题栏按钮热区加大到 32×28（`@media (pointer: coarse)`）；`color-scheme: light` meta 防 iOS 深色模式反色控件；body 级 `touch-action: manipulation` 防 iOS 双击缩放错位（iOS 10+ 忽略 user-scalable=no）；`.mine-board` 加 `-webkit-touch-callout:none` 保长按插旗 ④ 图标对比度重绘 5 枚（make_icons.py 重跑）：扫雷加浅色光晕、关机浅灰面琥珀符号、屏保浅灰机身、开始浅蓝窗面、展览馆藏青大门+深色内庭；**注意：Playwright WebKit 合成 tap 在全屏覆盖层上不派发事件（harness 局限，真机正常），屏保退出用例需手动 dispatch pointerdown 验证**。
- **2026-07-23｜扫雷体验修**（Playwright e2e 5 项断言全过）：模式更名「地下城」→「寻找时间胶囊」（MODE_DEFS label + 职业选择标题 + style 注释同步，模式 id 仍为 `dungeon`）；修 PC 切模式瞬间窗口不贴合——根因：`fitWindowToContent` 只在 startFloor（选完职业）执行，挂载到选职业这段窗口仍是上一模式尺寸（内容 294×433 被裁进 240×348）；现挂载即 fit，选职业前后窗口尺寸一致。
- **2026-07-23｜扫雷·地下城 2.0「记忆胶囊」**（PC/手机 Playwright e2e 10 项断言全过，含防误触回归）：三职业（维修技师/探测员/拾荒者，被动+每层 1 次主动技能，数据驱动 CLASSES）、装备（永久被动 5 种，三选一/商店获得）与一次性消耗品（📡🔮💊 数量制，显示=可用次数）分家；每层 1 宝箱（随机金币+35% 记忆碎片）、每过 2 层出商店（会员卡 -20%）；11 层主线——B11 Boss 层无出口须全清、踩雷伤害 2；结局按碎片（≥5）双判定：好结局=1998 年写给未来的信（记事本样式）、坏结局=□□残缺信，好结局达成后职业选择显示「💊 已解封」；格子 24→30px、楼层封顶 12×12（手机 390px 刚好）、棋盘下新增装备栏 belt（技能/消耗品/装备图标，toast 锚在棋盘面板内不盖 belt）；引擎 `remainingFlags` 计入 known（雷达标记即算发现）；排雷靴删除（与护甲趋同）。
- **2026-07-23｜扫雷防误触重置**（PC/手机 Playwright e2e 5 项断言全过）：对局进行中点难度按钮/切模式 Tab 先弹「放弃当前对局」确认框（`confirmGiveUp`，模态期间 app 根元素 inert 锁指针+键盘、默认焦点「取消」、遮罩加 `touch-action:none` 防滚动卷走）；未开局/已结束直接放行；点已激活 Tab 不再重置；笑脸仍是即点即重开。模式经 `ctx.setGuard(fn)` 向容器登记 hasProgress——经典=已开局未结束；地下城=本层已开局或已深入 B2+ 且未结束（B1 未开局无进度可丢）。
- **2026-07-23｜接力扩编**：Windows 机就位（`C:/Kimi Code/win98`，机器互认信息见 `COLLAB.md`「当前在役机器」）——clone 走 gh-proxy 镜像、push/pull 走 SSH 443（密钥方案，公钥已入 GitHub 账号）、Playwright Chromium 验收能力与 Mac 对齐；双机接力链路已实测（fetch/push 均通）。
- **2026-07-23｜扫雷双模式落地**（PC/手机 Playwright e2e 31 项断言全过）：① 引擎拆分——棋盘数据/首击安全/输入/绘制抽成共享引擎 `js/apps/mine-core.js`（`WIN98_MINE_CORE.createBoard`，钩子制：canInteract/onMineHit 可存活/onAllClear/onCellRevealed/onRevealedClick/decorateCell 等；`MODES` 模式注册表）；`js/apps/minesweeper.js` 改为容器（Tab 切模式，模式脚本缺失时 Tab 整行隐藏优雅降级）+ 经典模式 ② 经典模式新增自定义难度（宽 9–30/高 9–24/雷 10–(宽-1)×(高-1) 钳制，对话框雷数上限随宽高联动，自定义档不记最佳时间；配置存 `win98.mine.custom`）③ 新模式「地下城」= 扫雷×肉鸽（`js/apps/mine-dungeon.js`）：HP 制踩雷掉血、每层藏出口🕳️点它下楼、下楼三选一增益（数据驱动 POOL，局外成长留口子=抽卡前过滤池）、楼层尺寸雷密渐增、全清回血+自动下楼、最深纪录 `win98.mine.dungeon.deepest`；**注意：两模式 fitWindowToContent 锚点统一是容器根 `.app-mine`（含 Tab 行），锚模式内层根会少算 Tab 行高度**。
- **2026-07-23｜屏保加固三连修**（真实浏览器复测通过）：① 修无法退出——iframe 加 `pointer-events:none` 让事件穿透到覆盖层（iframe 内事件不冒泡到父页面，勿删）；② 修 PC 二次触发显示异常——`show()` 每次重设 `iframe.src`，首/N 次触发路径归一（规避隐藏 iframe 复显的 GPU 合成层怪癖，无头环境无法复现此类问题）；③ 屏保画面精简——`chrome=0` 时展品按 `bare` prop 只留纯特效 + 右下角署名（App.vue 透传，展品自行响应）。
- **2026-07-23｜展览馆入口确立**：桌面「展品 001」升级为博物馆入口「展览馆」（新像素图标 gallery.png），开窗即现代展厅大厅（暗色展品墙 + EXHIBIT 编号制）；展品元数据集中在 `exhibits/src/exhibits/manifest.js`——新展品 = 拷组件 + `src/exhibits/xxx.vue` + manifest 一条 + build，主站 `config.js` 零改动；`?ex=xxx` 直达单展品、`?chrome=0` 嵌入模式（屏保用）。
- **2026-07-23｜展品 001 作品化**：创作阐述居中渐显、组件署名挪角落小字、指针视差（Pointer Events）、点击夜空召唤一阵流星（独立 Meteors 实例，数秒后撤下）。
- **2026-07-23｜屏保框架落地（.scr 容器）**：`js/screensaver.js` 闲置 60s 全屏播放 `?ex=meteors&chrome=0`，任意输入退出（触发后 0.8s 宽限防误触退出；关机彩蛋播放中不触发；页面隐藏不计时）；开始菜单新增「屏幕保护」手动预览（saver.png 图标）。
- **2026-07-23｜展柜工程优化**：① inspira-ui 组件源本地镜像就位（浅克隆 `../tools/inspira-ui`，组件源在 `app/components/inspira/ui/<组件名>/`；官方 CLI 验证结论：shadcn-vue CLI + inspira registry 支持纯 Vite，但依赖体系重，本项目坚持镜像手工拷贝，结论详见 `exhibits/CANDIDATES.md` 待办区）② 展品页 `?ex=` 参数路由：`exhibits/src/App.vue` 改为路由壳，`import.meta.glob('./exhibits/*.vue')` 按参数动态加载，新展品零改壳代码；无参/未知参数自动列出全部展品。
- **2026-07-22｜扫雷模块修复+增强**：修掉 PC 端窗口默认尺寸总比内容小一截的问题（fitWindow 改为按棋盘实测尺寸反推窗口，勿再用估算常数）；修掉手机端中/高级横滚后左侧内容永远看不到的问题（`.app-mine` 弃用 `align-items:center` 负溢出，改 `margin-inline:auto` 居中）；新增 chord 快开（点已翻开数字格、周围旗数凑够时批量翻开）、按下 😮 反馈、每难度最佳时间记录（localStorage `win98.mine.best.*`）；雷区凹框改经典银灰底。
- **2026-07-22｜定名「旧电脑」**：新增主题宪章 `THEME.md`：确立"复古壳 × 现代芯"方向、slogan「机器会旧，人会老，想象力不会」、内容容器规划（文/画/歌/视频/屏保/游戏）、作品档案（右键属性）规范。
- **2026-07-22｜「现代的芯」双通道落地**：欢迎窗原生特效（星空跃迁 Canvas + 乱序解码标题，零依赖）＋ `exhibits/` 展柜通道（唯一允许构建的目录：Vite+Vue+Tailwind，dist 提交进 git；通用 iframe 渲染器 `js/apps/exhibit.js`，新展品只改 `config.js` 的 exhibit 路径；选题表 `exhibits/CANDIDATES.md`）。
- **2026-07-22｜URL 深链接**：`#open=模块id` 直接打开对应窗口（便于分享与自动化验收）。
- **2026-07-20｜图标全部自绘化**：微软 Win98 原版图标素材全部移除，替换为 `tools/make_icons.py` 程序生成的自绘像素图标（Pillow），零版权风险；加图标 = 写 `draw_xxx` → 注册 `ICONS` → 重跑。
- **2026-07｜基础版成型**：桌面图标渲染、窗口系统（拖拽/置顶/最小化/最大化/关闭/单例/右下角拖柄自由缩放）、任务栏（开始按钮、开始菜单、任务按钮、时钟）、关机彩蛋、手机端适配（窗口默认最大化、双触打开图标）就绪；模块：我的电脑(about)、扫雷(mine)、德州扑克(poker，1v1 AI、WebAudio 音效、战绩 localStorage)、友情链接(link → mihoyo.com)；已下架：记事本、计算器、回收站、留言本（渲染函数已移除，git 历史可恢复）；部署 GitHub Pages（https://viola0723.github.io/win98/，推送后 1-3 分钟自动更新）。
